import turtle

set_turtle=turtle.Turtle()
length=50
angle=90


window=turtle.Screen()
colors=[red, purple]
for c in colors:
  set_turtle c



for i in [angle]*4:
  set_turtle.forward(length)
  set_turtle.left(angle) 

set_turtle.up()
set_turtle.forward(100
                  )
for i in [angle]*4:
  set_turtle2.forward(length)
  set_turtle2.left(angle)  

window.exitonclick()